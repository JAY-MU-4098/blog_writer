version: "3.9"

services:
  blog-writer:
    build: .
    container_name: blog_writer_ai
    env_file:
      - .env
    ports:
      - "8000:8000"
    # Save generated blog HTML to host: ./generated_blog (same folder as where you run docker compose)
    volumes:
      - ./generated_blog:/app/generated_blog